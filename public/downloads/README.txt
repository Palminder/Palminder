Approved PDF downloads only.

Place an approved recruiter-pack PDF and CV PDF here, then reference them in
src/data/site.ts (site.downloads.recruiterPackPdf and site.downloads.cvPdf).
Nothing in this folder is linked until those values are set. Check every PDF's
metadata (author, title, comments) before adding it: see SANITISATION_CHECKLIST.md.
