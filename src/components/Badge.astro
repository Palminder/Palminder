---
interface Props {
  variant: string;
  label: string;
  class?: string | undefined;
}

const { variant, label, class: className } = Astro.props;
---

<span class:list={['badge', `badge--${variant}`, className]}>{label}</span>
