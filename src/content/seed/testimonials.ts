import type { Testimonial } from '@/lib/content/types';

/**
 * No testimonial is seeded. The site launches without a testimonial section unless
 * genuine, approved material exists; the component renders verified records only.
 */
export const testimonials: Testimonial[] = [];
